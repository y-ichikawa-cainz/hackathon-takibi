```markdown
# タイトル: 従業員情報画面に「趣味・嗜好」入力欄を追加

## 要約: 従業員情報に「趣味・嗜好」を登録できる機能を追加し、人事部や総務部が従業員の興味関心を把握できるようにする。これにより、福利厚生の企画立案を効率化する。

## ユーザーストーリー:
- **[誰が]**: 人事部、総務部、福利厚生を担当する部署、および全従業員
- **[何のために]**: 福利厚生の企画立案を効率化するため、従業員の興味関心を把握するため
- **[何ができる]**: 従業員は自分の趣味・嗜好を登録でき、管理側は一覧・検索できる

## 開始条件:
- [x] 開発環境のセットアップが完了していること。
- [x] 従業員情報管理システムの基本的な機能が動作していること。

## 完了条件:
- [x] 従業員が自分の趣味・嗜好を登録できること。
- [x] 管理側が従業員の趣味・嗜好を一覧で確認できること。
- [x] 管理側が従業員の趣味・嗜好を検索できること。

## 実実装内容:
> 修正・新規作成が必要なモジュールとロジックの要点

1. **データ構造の変更:**
    * `src/employee_master.py` に関連するCSVファイル（`employees.csv`）に「趣味・嗜好」カラムを追加する。

2. **従業員追加処理の修正:**
    * `src/employee_master.py` の `add_employee()` 関数を修正し、「趣味・嗜好」の入力を求めるようにする。

        ```python
        # src/employee_master.py (修正箇所)
        def add_employee():
            emp_id = input('従業員ID: ')
            name = input('名前: ')
            dept = input('部署: ')
            org_id = input('所属組織ID: ')
            hobby = input('趣味・嗜好: ')  # ★追加
            with open(EMPLOYEE_CSV, 'a', newline='', encoding='utf-8') as f:
                writer = csv.writer(f)
                writer.writerow([emp_id, name, dept, org_id, hobby]) # ★修正
            print('従業員を追加しました。')
        ```

3. **従業員一覧表示処理の修正:**
    * `src/employee_master.py` の `list_employees()` 関数を修正し、「趣味・嗜好」も表示するようにする。

        ```python
        # src/employee_master.py (修正箇所)
        def list_employees():
            if not os.path.exists(EMPLOYEE_CSV):
                print('従業員データがありません。')
                return
            with open(EMPLOYEE_CSV, 'r', encoding='utf-8') as f:
                reader = csv.reader(f)
                print('ID\t名前\t部署\t所属組織ID\t趣味・嗜好') # ★修正
                for row in reader:
                    print('\t'.join(row))
        ```

4. **検索機能の追加:**
   * `src/employee_master.py` に、趣味・嗜好をキーワードで検索できる関数を追加する。  例：`search_employee_by_hobby()`

        ```python
        # src/employee_master.py (追加箇所)
        def search_employee_by_hobby(keyword):
            employees = []
            if not os.path.exists(EMPLOYEE_CSV):
                print('従業員データがありません。')
                return employees
            with open(EMPLOYEE_CSV, 'r', encoding='utf-8') as f:
                reader = csv.reader(f)
                for row in reader:
                    if keyword.lower() in row[4].lower(): #趣味・嗜好カラムを検索
                        employees.append(row)
            return employees

        def main():
            while True:
                print('\n1: 従業員追加  2: 一覧表示  3: 趣味検索 9: 終了') # ★修正
                cmd = input('番号を選択: ')
                if cmd == '1':
                    add_employee()
                elif cmd == '2':
                    list_employees()
                elif cmd == '3': # ★追加
                    keyword = input('検索キーワード(趣味・嗜好): ')
                    results = search_employee_by_hobby(keyword)
                    if results:
                        print('検索結果:')
                        print('ID\t名前\t部署\t所属組織ID\t趣味・嗜好')
                        for row in results:
                            print('\t'.join(row))
                    else:
                        print('該当する従業員はいません。')

                elif cmd == '9':
                    print('終了します')
                    break
                else:
                    print('無効な入力です')
        ```

5. **UIの変更（もしUIが存在する場合）:**
    * Web UIやGUIが存在する場合、趣味・嗜好の入力フィールドと表示領域を追加する。 (このチケットでは、コンソールアプリケーションなのでUIの変更は考慮しない)

**補足事項:**
* CSVファイルのヘッダー行も修正する必要がある。既存のCSVファイルにデータがある場合、カラム追加後にデータ形式が崩れないように注意する。

## ストーリーポイント:
- **予測値**: 3
```
